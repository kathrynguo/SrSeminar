//organizing presenters

public class SrPresenter {
	
	public SrPresenter(String seminar, String seminarID, String name, String gradYear) {
		
		System.out.println(seminar + " " + seminarID + ", " + name + " '" + gradYear); 
	}
}
